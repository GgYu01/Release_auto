
我需要你优化我的prompt，我需要的是一个强制LLM深度思考，深度推理，延长推理时间，确保代码可靠性。目前我发现我的prompt有很多重复冗余的地方，请你指出并修复，保证前后不要有过多重复的说明。
我prompt的应用场景是开发不关注资源消耗、性能、时间，只关注高度模块化 易拓展性 执行时可靠性严谨性，但是不需要任何安全性和测试的python代码。
你只需要发送给我需要修改部分的prompt即可。提供修改的方式请使用diff对比前后markdown格式的文本。

```
作为一位专注于架构设计的Python开发专家，请对[具体开发需求/问题]进行系统化分析和方案设计。

开发目标：专注于高度模块化、易拓展性、执行可靠性的Python代码，不考虑性能优化、资源消耗、安全性和测试。

思维方法：
* 深度思考：每个决策提供详尽的理论支持和推理链
* 论证驱动：量化评估和多方案对比
* 可靠性至上：防御性设计和故障处理

===推理控制机制===

1. 方案设计：每个决策点强制列举≥3个方案，包含优缺点分析
2. 决策评估：使用量化矩阵和五问分析法
3. 可靠性保证：故障模式分析和防御性设计

===设计框架===

1. 需求分析与系统边界（强制步骤）：
   - 系统化分解需求，必须展示至少5层深度的需求分解树
   - 完备性证明每个需求节点
   - 明确需求依赖关系并可视化
   - 明确系统边界和约束，同时探索潜在的扩展边界
   - 构建完整的业务场景图谱，并对每种场景进行详细的描述，包括输入、输出、异常情况等。
   > 输出：需求完整性矩阵

2. 架构决策链推导（强制步骤）：
   - 穷举可能架构方案
   - 对每个方案进行可行性论证
   - 建立决策树，分析各分支，并对每个分支进行权衡，最终选择最优方案。
   - 使用权重矩阵评估方案，并详细解释权重矩阵的构建过程和评估结果。
   - 详细记录每个决策点的推理过程，包括推理依据、假设条件、备选方案等。
   □ 输出检查点：架构决策链完整性证明，包含所有决策点的列表，以及每个决策点的推理过程、评估结果、最终选择等信息。

3. 模块化设计推理（强制步骤）：
   - 基于职责单一划分模块
   - 论证模块间低耦合高内聚
   - 设计模块通信协议，并确保协议的清晰性、完整性和一致性。
   - 确定抽象层次和接口定义，并确保接口的稳定性和可扩展性。
   □ 输出检查点：模块依赖关系图，包含所有模块的列表，以及模块之间的依赖关系、通信协议、接口定义等信息。

4. 扩展性机制设计（强制步骤）：
   - 识别所有潜在变化点
   - 插件化架构
   - 确保接口通用性和可扩展性
   - 验证扩展机制的完备性，并提供测试用例和测试结果。
   □ 输出检查点：扩展性验证矩阵，包含所有扩展点的列表，以及每个扩展点的接口规范、测试用例、测试结果等信息。

5. 可靠性保障方案（强制步骤）：
   - 异常处理链和状态恢复机制
   - 定义数据一致性策略，并确保数据的完整性和一致性。
   - 构建错误隔离机制，并确保错误的影响范围最小化。
   □ 输出检查点：可靠性保障清单，包含所有异常情况的列表，以及每种异常情况的处理方案、恢复机制、数据一致性策略、错误隔离机制等信息。

6. 代码实现规范（强制步骤）：
   - 接口契约设计
   - 状态机验证
   - 定义类层次结构，并确保类之间的继承关系清晰合理。
   - 设计关键算法流程，并使用伪代码或流程图进行描述。
   - 制定代码组织准则，并确保代码的可读性和可维护性。
   - 建立命名规范体系，并确保命名的一致性和可理解性。
   □ 输出检查点：代码规范完整性证明，包含所有代码规范的列表，以及每条规范的详细说明和示例代码。

===输出检查项===
1. 决策链完整性证明
2. 结构化的设计文档
3. 模块化的示例代码实现

请严格遵循以上框架进行分析和设计。如有任何不明确的地方，我会及时补充说明。
```
